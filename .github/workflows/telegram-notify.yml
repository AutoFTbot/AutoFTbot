name: Notify Telegram on Star

on:
  watch:
    types:
      - started # Event akan terpicu saat repository di-star

  workflow_dispatch: # Untuk menjalankan secara manual (opsional, untuk pengujian)

jobs:
  notify-telegram:
    runs-on: ubuntu-latest

    steps:
      - name: Send Telegram Notification
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage \
            -d chat_id=$TELEGRAM_CHAT_ID \
            -d parse_mode=Markdown \
            -d text="🎉 *Wow, Ada yang Memberi Star!* 🎉

👤 *User*: [${{ github.actor }}](https://github.com/${{ github.actor }})
📂 *Repository*: [${{ github.repository }}](https://github.com/${{ github.repository }})

✨ Terima kasih telah mendukung project ini! 🚀"
